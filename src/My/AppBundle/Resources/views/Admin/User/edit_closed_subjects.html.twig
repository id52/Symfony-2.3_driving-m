{% extends 'AppBundle::admin.html.twig' %}


{% block content %}
    <h1>Запрещённые предметы</h1>
    {{ user.lastName }} {{ user.firstName }} {{ user.patronymic }}
    <hr>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="control-group">
        <div class="controls">
            <button type="submit" class="btn btn-success">Сохранить</button>
            <a class="btn btn-default pull-right" href="{{ path('admin_users_with_closed_subjects') }}"><i class="fa fa-arrow-left"></i> Назад</a>
        </div>
    </div>
    {{ form_end(form) }}
{% endblock content %}
